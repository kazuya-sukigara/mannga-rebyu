<% @manngas.each do |mannga| %>
	<%= mannga.id %>
	<%= link_to mannga.name, admin_mannga_path(mannga) %>
	<%= mannga.author%>
<% end %>
<%= link_to "漫画登録",new_admin_mannga_path, class: "btn-lg btn-primary" %>


